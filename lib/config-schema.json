{
  "testDirName": {
    "description":
      "Set the directory name for where the test template will be saved to. If not set, the test will be saved to the current directory",
    "type": "string",
    "default": ""
  },
  "testSufix": {
    "description": "Appends a string to your test file E.G: 'myfile.test.js'",
    "type": "string",
    "default": ".test"
  },
  "typeSystem": {
    "description":
      "Specify if the code the tests are being generated for contain a type system I.E flow/tyescript",
    "type": "string",
    "default": "",
    "enum": [
      { "value": "", "description": "None" },
      { "value": "FLOW", "description": "Flow" },
      { "value": "TYPESCRIPT", "description": "TypeScript" }
    ]
  }
}
